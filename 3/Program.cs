using static Sorting;
using static Infrastructure;

// int[] array = CreateArray(10);
// Show(array);
// SortSelection(array);
// Show(array);

10.CreateArray(min: 2, max: 10)
  .Show()
  .SortSelection()
  .Show();
